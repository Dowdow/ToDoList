{% extends '::base.html.twig' %}

{%  block body %}
    <h1>Add a list</h1>
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    <input type="submit" />
    {{ form_end(form) }}
{% endblock %}
